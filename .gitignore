/target/
/release/
.env
.DS_Store

# Local dev sandbox: .cargo/config.toml points cargo runs at a throwaway
# journal in .dev/ on this machine only. Nothing here is committed.
/.cargo/config.toml
/.dev/
